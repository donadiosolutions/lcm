---
"@donadiosolutions/lcm": patch
---

Package and verify the Linux native daemon-restart helper during builds. The helper performs bounded descriptor admission and a non-mutating OpenStable handshake, but cannot restart or recover a daemon.
