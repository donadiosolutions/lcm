---
"@donadiosolutions/lcm": patch
---

Package and verify the Linux native daemon-restart helper during builds. The helper currently performs only bounded read-only admission and cannot restart or recover a daemon.
