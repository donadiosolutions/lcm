---
"@donadiosolutions/lcm": patch
---

Keep nested `lcm connectors install` on ordinary root migration and
connector-owned verification instead of applying the top-level installer's
publication-convergence retry.
