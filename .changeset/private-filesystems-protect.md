---
"@donadiosolutions/lcm": patch
---

Protect local memory files with private permissions, safe atomic metadata writes, contained instruction reads, private hashed restore locks, stable project aliases, and a transcript allowlist that cannot be widened by using the filesystem root.
