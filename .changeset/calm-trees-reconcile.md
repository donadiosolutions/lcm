---
"@donadiosolutions/lcm": patch
---

Keep worktree reconciliation available when an unrelated stale project-map path crosses a regular file, while preserving strict filesystem validation for the requested project.
