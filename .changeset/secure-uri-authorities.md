---
"@donadiosolutions/lcm": patch
---

Update the bundled URI parser to a patched release that prevents backslash authority host confusion while preserving the deduplicated MCP SDK and AJV build graph.
