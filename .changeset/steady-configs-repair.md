---
"@donadiosolutions/lcm": patch
---

Honor legacy compaction candidate limits across stored and runtime configuration, and let `lcm doctor` repair Claude settings files whose JSON root is not an object.
