---
"@donadiosolutions/lcm": major
---

Replace `lcm map` with unified machine registration and project identity commands, preserving local SQLite hashes while adding explicit PostgreSQL UUIDv7 project pairing and reimage recovery.
