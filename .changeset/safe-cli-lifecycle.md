---
"@donadiosolutions/lcm": patch
---

Prevent nested daemon, config, map, and connector help flags from executing command actions, await unknown-command output before the CLI settles, and honor the Ninja renderer's one-shot `onReady` callback.
