---
"@donadiosolutions/lcm": patch
---

Allow managed Linux daemons to launch process-provider CLIs installed alongside LCM without inheriting an untrusted shell path, and align doctor checks with the managed daemon environment.
