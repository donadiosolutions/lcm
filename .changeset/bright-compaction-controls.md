---
"@donadiosolutions/lcm": minor
---

Add configurable OpenAI-compatible timeouts and retries, safe configuration get/set and daemon restart commands, model environment overrides, and accurate batch compaction token totals.
