---
"@donadiosolutions/lcm": patch
---

Preserve nested non-file URLs after a pathless `file://` query or fragment
boundary while continuing to redact standalone and nested filesystem paths,
including quoted paths containing spaces.
