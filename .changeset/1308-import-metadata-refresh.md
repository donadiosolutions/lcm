---
"@donadiosolutions/lcm": patch
---

Preserve concurrent import metadata and retry identities by merging portable
knowledge convergence into the current stored row instead of a stale import
scan snapshot.
