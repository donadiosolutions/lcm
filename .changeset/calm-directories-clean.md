---
"@donadiosolutions/lcm": patch
---

Preserve private-directory authentication failures when descriptor cleanup
also fails, reporting both errors while keeping the authentication failure as
the cause.
