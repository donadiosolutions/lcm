---
"@donadiosolutions/lcm": patch
---

Replace alias-oriented generated agent instructions with seven compact canonical tag conventions and one concrete store example, and clarify that `signal:memory_used` requires a paired `memory_id:<id>` tag for recall usage counting.
