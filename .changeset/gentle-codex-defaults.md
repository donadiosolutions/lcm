---
"@donadiosolutions/lcm": patch
---

Install Codex hooks, skill, and rules by default, write Codex hooks to `~/.codex/hooks.json`, and make the Codex rules connector idempotently maintain `~/.codex/AGENTS.md`.
