---
"@donadiosolutions/lcm": patch
---

Add `lcm events promote --all` to drain queued passive-learning events from all metadata-backed sidecars, and update doctor messaging to recommend that command when the daemon is healthy but old sidecar backlogs remain.
