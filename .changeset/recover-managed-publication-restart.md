---
"@donadiosolutions/lcm": patch
---

Allow an explicit daemon restart to recover an exact managed service that owns backend publication but produces no HTTP response, while preserving fail-closed config, journal, process, and manager identity checks.
